class Carro{ /*Esta es la forma en la que se declara una clase en js*/

    constructor(license, driver){
        this.id
        this.license = license;
        this.driver = driver;
        this.pasajero
    }

    imrprimeCarro(){
        console.log(this.driver)
        console.log(this.driver.name)
        console.log(this.driver.document)
    }
    
}